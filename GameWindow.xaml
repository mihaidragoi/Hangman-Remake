<Window x:Class="Hangman.View.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Hangman.ViewModel"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="GameWindow" Height="550" Width="850">

    <Window.InputBindings>
        <KeyBinding Modifiers="Control" Key="N" Command="{Binding NewGameCommand}"/>
        <KeyBinding Modifiers="Control" Key="O" Command="{Binding OpenGameCommand}"/>
        <KeyBinding Modifiers="Control" Key="S" Command="{Binding SaveGameCommand}"/>

        <KeyBinding Key="A" Command="{Binding GuessLetterCommand}" CommandParameter="A"/>
        <KeyBinding Key="B" Command="{Binding GuessLetterCommand}" CommandParameter="B"/>
        <KeyBinding Key="C" Command="{Binding GuessLetterCommand}" CommandParameter="C"/>
        <KeyBinding Key="D" Command="{Binding GuessLetterCommand}" CommandParameter="D"/>
        <KeyBinding Key="E" Command="{Binding GuessLetterCommand}" CommandParameter="E"/>
        <KeyBinding Key="F" Command="{Binding GuessLetterCommand}" CommandParameter="F"/>
        <KeyBinding Key="G" Command="{Binding GuessLetterCommand}" CommandParameter="G"/>
        <KeyBinding Key="H" Command="{Binding GuessLetterCommand}" CommandParameter="H"/>
        <KeyBinding Key="I" Command="{Binding GuessLetterCommand}" CommandParameter="I"/>
        <KeyBinding Key="J" Command="{Binding GuessLetterCommand}" CommandParameter="J"/>
        <KeyBinding Key="K" Command="{Binding GuessLetterCommand}" CommandParameter="K"/>
        <KeyBinding Key="L" Command="{Binding GuessLetterCommand}" CommandParameter="L"/>
        <KeyBinding Key="M" Command="{Binding GuessLetterCommand}" CommandParameter="M"/>
        <KeyBinding Key="N" Command="{Binding GuessLetterCommand}" CommandParameter="N"/>
        <KeyBinding Key="O" Command="{Binding GuessLetterCommand}" CommandParameter="O"/>
        <KeyBinding Key="P" Command="{Binding GuessLetterCommand}" CommandParameter="P"/>
        <KeyBinding Key="Q" Command="{Binding GuessLetterCommand}" CommandParameter="Q"/>
        <KeyBinding Key="R" Command="{Binding GuessLetterCommand}" CommandParameter="R"/>
        <KeyBinding Key="S" Command="{Binding GuessLetterCommand}" CommandParameter="S"/>
        <KeyBinding Key="T" Command="{Binding GuessLetterCommand}" CommandParameter="T"/>
        <KeyBinding Key="U" Command="{Binding GuessLetterCommand}" CommandParameter="U"/>
        <KeyBinding Key="V" Command="{Binding GuessLetterCommand}" CommandParameter="V"/>
        <KeyBinding Key="W" Command="{Binding GuessLetterCommand}" CommandParameter="W"/>
        <KeyBinding Key="X" Command="{Binding GuessLetterCommand}" CommandParameter="X"/>
        <KeyBinding Key="Y" Command="{Binding GuessLetterCommand}" CommandParameter="Y"/>
        <KeyBinding Key="Z" Command="{Binding GuessLetterCommand}" CommandParameter="Z"/>
    </Window.InputBindings>


    <Grid Background="LemonChiffon">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left" Background="Bisque" FontWeight="Bold" Padding="3">

            <MenuItem Header="File">
                <MenuItem Header="New Game" Command="{Binding NewGameCommand}" InputGestureText="Ctrl+N"/>
                <MenuItem Header="Open Game" Command="{Binding OpenGameCommand}" InputGestureText="Ctrl+O"/>
                <MenuItem Header="Save Game" Command="{Binding SaveGameCommand}" InputGestureText="Ctrl+S"/>
                <MenuItem Header="Statistics" Command="{Binding StatisticsCommand}"/>
                <MenuItem Header="Cancel" Command="{Binding CancelCommand}"/>
            </MenuItem>
            <MenuItem Header="Categories">
                <MenuItem Header="All categories" IsCheckable="True" IsChecked="{Binding IsAllSelected, Mode=OneWay}" Command="{Binding SelectCategoryCommand}" CommandParameter="All categories"/>
                <MenuItem Header="Cars" IsCheckable="True" IsChecked="{Binding IsCarsSelected, Mode=OneWay}" Command="{Binding SelectCategoryCommand}" CommandParameter="Cars"/>
                <MenuItem Header="Movies" IsCheckable="True" IsChecked="{Binding IsMoviesSelected, Mode=OneWay}" Command="{Binding SelectCategoryCommand}" CommandParameter="Movies"/>
                <MenuItem Header="States" IsCheckable="True" IsChecked="{Binding IsStatesSelected, Mode=OneWay}" Command="{Binding SelectCategoryCommand}" CommandParameter="States"/>
                <MenuItem Header="Mountains" IsCheckable="True" IsChecked="{Binding IsMountainsSelected, Mode=OneWay}" Command="{Binding SelectCategoryCommand}" CommandParameter="Mountains"/>
                <MenuItem Header="Rivers" IsCheckable="True" IsChecked="{Binding IsRiversSelected, Mode=OneWay}" Command="{Binding SelectCategoryCommand}" CommandParameter="Rivers"/>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About" Command="{Binding AboutCommand}"/>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                    <Border BorderBrush="Black" BorderThickness="1" Margin="0,0,10,0">
                        <Image Source="{Binding PlayerAvatar}" Width="50" Height="50" Stretch="UniformToFill"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding PlayerName}" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text="{Binding CurrentLevelDisplay}" FontSize="14" Foreground="DarkSlateGray"/>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Row="1" Background="White" BorderBrush="Black" BorderThickness="2" Margin="10" Height="250">
                    <Image Source="{Binding HangmanImage}" 
           Stretch="UniformToFill" 
           HorizontalAlignment="Left" 
           VerticalAlignment="Top"/>
                </Border>

                <StackPanel Grid.Row="2" Margin="20,20,20,20" VerticalAlignment="Top" Grid.ColumnSpan="2" HorizontalAlignment="Left">
                    <TextBlock Text="{Binding SelectedCategory}" FontSize="22" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Left"/>

                    <ItemsControl ItemsSource="{Binding WordDisplayList}" HorizontalAlignment="Left" MaxWidth="800">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Left" Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Black" BorderThickness="0,0,0,2" Width="22" Margin="2,0,4,5">
                                    <TextBlock Text="{Binding}" HorizontalAlignment="Center" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Height="23"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Grid>

            <Grid Grid.Column="1" Margin="20,0,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="Bisque" CornerRadius="10" Padding="15" Margin="0,10,0,5" 
            BorderBrush="BurlyWood" BorderThickness="2">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Time Remaining: " FontSize="12" FontWeight="Bold" Foreground="#444"/>
                        <TextBlock Text="{Binding timeToDisplay}" FontSize="12" FontWeight="Bold" Foreground="DarkBlue"/>
                    </StackPanel>
                </Border>

                <ItemsControl Grid.Row="1" ItemsSource="{Binding ErrorSlots}" HorizontalAlignment="Center" Margin="0,10">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="35" Height="35" Margin="3" Background="White" BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
                                <TextBlock Text="{Binding}" VerticalAlignment="Center" HorizontalAlignment="Center" 
                               Foreground="Red" FontWeight="Bold" FontSize="20"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="10" Margin="0,5" VerticalAlignment="Top" MaxWidth="500">
                    <ItemsControl ItemsSource="{Binding Alphabet}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="9"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Character}" 
                            IsEnabled="{Binding IsAvailable}" 
                            Command="{Binding DataContext.GuessLetterCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}" 
                            Margin="3" Height="40" FontWeight="Bold" FontSize="14"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>