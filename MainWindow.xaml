<Window x:Class="Hangman.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hangman"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Sign in..." Height="450" Width="800">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Beige"/>
            <Setter Property="Foreground" Value="#2F4F4F"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="CornflowerBlue" 
                                BorderThickness="1" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="CornflowerBlue"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#AAFFFFFF"/>
            <Setter Property="BorderBrush" Value="CornflowerBlue"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border CornerRadius="10" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Margin="0" Focusable="false">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>


    <Grid Background="Wheat">

        <Button Content="New User" HorizontalAlignment="Left" Margin="66,363,0,0" VerticalAlignment="Top" Height="35" Width="86" Command="{Binding NewUserCommand}" Background="Beige"/>
        <Button Content="Delete User" HorizontalAlignment="Left" Margin="224,363,0,0" VerticalAlignment="Top" Height="35" Width="74" Command="{Binding DeleteUserCommand}" IsEnabled="{Binding EnabledButtons}" Background="Beige"/>
        <Button Content="Play" HorizontalAlignment="Left" Margin="572,363,0,0" VerticalAlignment="Top" Height="35" Width="60" Command="{Binding PlayCommand}" IsEnabled="{Binding SelectedUser}" Background="Beige"/>
        <Button Content="Cancel" HorizontalAlignment="Left" Margin="696,363,0,0" VerticalAlignment="Top" Height="35" Width="51" Command="{Binding CancelCommand}" Background="Beige"/>

        <ListBox ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" DisplayMemberPath="Name" Margin="50,59,502,121" Background="Beige" FontStyle="Italic"/>

<Border HorizontalAlignment="Left"  VerticalAlignment="Top"  Margin="510,100,0,0"  Height="120" Width="120" CornerRadius="60"  BorderBrush="White"  BorderThickness="4" Background="#EEE">
    
    <Border.Effect>
        <DropShadowEffect BlurRadius="15" ShadowDepth="0" Color="Black" Opacity="0.3"/>
    </Border.Effect>

            <Image Source="{Binding DisplayedAvatarPath}" Stretch="UniformToFill">

                <Image.Clip>
                    <EllipseGeometry Center="60,60" RadiusX="60" RadiusY="60"/>
                </Image.Clip>

            </Image>
        </Border>
        
        <Button Content="Previous" HorizontalAlignment="Left" Margin="487,245,0,0" VerticalAlignment="Top" Width="60" Command="{Binding PreviousCommand}" Background="Beige"/>
        <Button Content="Next" HorizontalAlignment="Left" Margin="602,245,0,0" VerticalAlignment="Top" Width="60" Command="{Binding NextCommand}" Background="Beige"/>

        <Border Background="#FDF5E6" CornerRadius="10"  Padding="15,5" HorizontalAlignment="Center"  VerticalAlignment="Top"  Margin="0,10,0,0" BorderBrush="CornflowerBlue"  BorderThickness="0,0,0,3">
            
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
            </Border.Effect>
            
            <TextBlock Text="Welcome to Hangman Game! Enjoy..." Foreground="CornflowerBlue"  FontSize="20"  FontWeight="Bold" TextAlignment="Center"/>
            
        </Border>

    </Grid>
</Window>
